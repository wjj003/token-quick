# a  sample of solidity
    mkdir hardhat-tutorial 
    cd hardhat-tutorial 
    npm init --yes 
    npm install --save-dev hardhat
    npx hardhat
        chose Create an empty hardhat.config.js
    compile
        npx hardhat compile
    run 
        npx hardhat test